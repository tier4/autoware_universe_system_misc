## TIER IV universe / split mirror (system & misc)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe`, `system/`, `vehicle/`, `evaluator/`, `simulator/`, and `visualization/` directories, into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See other branches for source codes.
